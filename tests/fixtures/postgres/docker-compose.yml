
services:
    postgresql:
        # init: true
        image: postgres:latest
        ports:
            - "5432:5432"
        environment:
            - POSTGRES_PASSWORD=postgres
        volumes:
            - /var/lib/postgresql